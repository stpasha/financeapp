spring:
  application:
    name: account-service
  cloud:
  liquibase:
    enabled: false
  flyway:
    default-schema: ${SPRING_DATASOURCE_ACCOUNT_SCHEMA:"account_info"}
    schemas: ${SPRING_DATASOURCE_ACCOUNT_SCHEMA:"account_info"}
    baseline-on-migrate: true
    baseline-version: 1
keycloak:
  server-url: http://keycloak:7080/
  realm: zbank
  username: gatekeeper
  password: swordfish
scheduler:
  enabled: false