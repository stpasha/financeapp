spring:
  application:
    name: account-service
  cloud:
  liquibase:
    enabled: false
    duplicateFileMode: WARN
liquibase:
  duplicateFileMode: WARN
keycloak:
  server-url: http://keycloak:7080/
  realm: zbank
  username: gatekeeper
  password: swordfish
scheduler:
  enabled: false